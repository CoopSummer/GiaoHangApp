import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        margin: 5,
        borderRadius: 40,
        borderWidth: 3,
        borderColor: '#ae22e0',
    },

    hinh: {
         borderRadius: 40,
         borderWidth: 1,
         borderColor: '#ffffff',
    },
 });

 export default styles;