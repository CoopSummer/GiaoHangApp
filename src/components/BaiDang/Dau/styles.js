import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
    },
    ten: {
        alignSelf: 'center',
        fontWeight: 'bold',
        color: '#3c3c3c'
    }
});

export default styles